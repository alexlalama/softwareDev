import random
def test_check_guess_range_low():
    assert check_guess(2, 8) == 9
    
    
def check_guess(guess, answer):
    
    '''
    answer = random.randint(1, 10)
    guess = int(input("Enter number 1 - 10"))
    while(guess != answer):
        if(guess >=10 ):
            return "Out of range"
        elif (guess != 10):
            return 'Guess again'
        else: 
            return 0
    ''' 

    return ''
    

